import { CourseCard } from '@/components/CourseCard'; import { getCourses, getCycles } from '@/lib/data';
export default async function CyclePage({ params }: { params: Promise<{ cycleId: string }> }) { const { cycleId } = await params; const [courses, cycles] = await Promise.all([getCourses(cycleId), getCycles()]); const cycle = cycles.find(c => String(c.id) === cycleId); return <section><div className="mb-6 text-white"><p className="text-sm font-bold text-gold">CICLO {cycle?.number ?? ''}</p><h1 className="text-3xl font-black">{cycle?.name ?? 'Cursos'}</h1></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{courses.map(c => <CourseCard key={c.id} course={c} />)}</div>{!courses.length && <p className="panel">No hay cursos disponibles para este ciclo todavía.</p>}</section> }
