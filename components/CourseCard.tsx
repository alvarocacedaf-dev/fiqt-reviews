import Link from 'next/link'; import type { Course } from '@/lib/types';
export function CourseCard({ course }: { course: Course }) { return <Link href={`/cursos/${course.id}`} className="surface-card-interactive block p-5"><p className="text-xs font-bold text-royal">{course.code ?? 'CURSO'}</p><h3 className="mt-1 font-bold text-ink">{course.name}</h3><p className="mt-2 text-sm text-slate-500">{course.credits ? `${course.credits} créditos` : 'Créditos por confirmar'}</p></Link> }
