export function VerificationStatus({ status }: { status: 'unverified' | 'pending' | 'verified' | 'rejected' }) { const text = { unverified: 'Sin verificar', pending: 'En revisión', verified: 'Verificado', rejected: 'Requiere cambios' }[status]; const color = status === 'verified' ? 'bg-emerald-100 text-emerald-800' : status === 'rejected' ? 'bg-red-100 text-red-800' : 'bg-amber-100 text-amber-800'; return <span className={`rounded-full px-3 py-1 text-sm font-bold ${color}`}>{text}</span> }
