'use client';
import { useState } from 'react'; import { createClient } from '@/lib/supabase/client'; import { useRouter } from 'next/navigation';
export function AuthForm({ mode }: { mode: 'login' | 'register' }) { const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const router = useRouter(); async function submit(form: FormData) { setLoading(true); setError(''); const db = createClient(); const email = String(form.get('email')); const password = String(form.get('password')); const result = mode === 'login' ? await db.auth.signInWithPassword({ email, password }) : await db.auth.signUp({ email, password, options: { data: { full_name: String(form.get('full_name')) } } }); setLoading(false); if (result.error) return setError(result.error.message); if (mode === 'register') setError('Revisa tu correo para confirmar la cuenta, si tu proyecto exige confirmación.'); else router.push('/ciclos'); } return <form action={submit} className="space-y-4">{mode === 'register' && <label className="block text-sm font-semibold">Nombre completo<input required name="full_name" className="input mt-1" /></label>}<label className="block text-sm font-semibold">Correo<input required type="email" name="email" className="input mt-1" autoComplete="email" /></label><label className="block text-sm font-semibold">Contraseña<input required minLength={8} type="password" name="password" className="input mt-1" autoComplete={mode === 'login' ? 'current-password' : 'new-password'} /></label>{error && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading ? 'Procesando…' : mode === 'login' ? 'Iniciar sesión' : 'Crear cuenta'}</button></form> }
