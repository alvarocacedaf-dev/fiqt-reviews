import Image from 'next/image'; import Link from 'next/link'; import type { Professor, Review } from '@/lib/types'; import { RatingSummary } from './RatingSummary';
export function ProfessorCard({ professor, courseName, reviews = [] }: { professor: Professor; courseName: string; reviews?: Review[] }) { return <article className="relative overflow-hidden rounded-[2rem] border-4 border-white bg-gradient-to-br from-white via-blue-50 to-blue-100 p-5 shadow-card"><div className="absolute right-4 top-3 text-3xl text-gold">♠</div><div className="flex gap-4"><div className="relative h-20 w-20 shrink-0 overflow-hidden rounded-2xl bg-blue-200">{professor.photo_url ? <Image src={professor.photo_url} fill className="object-cover" alt={`Foto de ${professor.full_name}`} /> : <span className="grid h-full place-items-center text-3xl text-royal">♙</span>}</div><div><h3 className="pr-7 text-lg font-black text-ink">{professor.full_name}</h3><p className="text-sm text-slate-600">{courseName}</p><p className="mt-1 text-xs text-slate-500">Fuente: {professor.source_name || 'DIRCE UNI'}</p></div></div><div className="my-5 border-y border-blue-100 py-3"><RatingSummary reviews={reviews} /></div><Link className="btn-primary w-full" href={`/profesores/${professor.id}`}>Ver perfil</Link></article> }
